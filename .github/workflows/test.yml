name: clang-tidy-review

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - uses: actions/github-script@v8
      with:
        script: |
          core.setFailed('hi this is the error message');
