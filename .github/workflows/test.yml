name: clang-tidy-review

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - uses: pajlada/test-github-action@master
      id: review

    - uses: pajlada/test-github-action/post_comments@master
